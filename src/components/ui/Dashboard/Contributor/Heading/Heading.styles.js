"use client";

import { makeStyles } from "tss-react/mui";

const useStyles = makeStyles()((theme) => ({
  heading: {
    fontSize: "2.2rem",
    fontWeight: 500,
  },
}));

export default useStyles;
