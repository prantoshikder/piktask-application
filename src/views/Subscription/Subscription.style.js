"use client";

import { makeStyles } from "tss-react/mui";

const useStyles = makeStyles()((theme) => ({
  headerText: {
    height: "200px", 
    textAlign: "center", 
    paddingTop: "80px", 
},
}));

export default useStyles;
